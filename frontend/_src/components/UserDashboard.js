import React from 'react';

const UserDashboard = () => {
  return (
    <section>
      <h2>Welcome back, User!</h2>
      {/* Display user-specific content here (e.g., bookings, wishlist) */}
    </section>
  );
};

export default UserDashboard;
